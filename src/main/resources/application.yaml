server:
  port: 8080


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/app_demo_be
    username: postgres
    password: 123123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#  cấu hình mail gửi otp
  mail:
    host: smtp.gmail.com
    port: 587
    username: huynhnhutduong7112003@gmail.com # gmail của sender
    password: mval orll zkxu ewim # quản lý tk gg -> xác thực 2 lớp -> app password -> create -> copy + paste pass
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  # cấu hình redis
  redis:
    host: localhost
    port: 6379
    password: 123456
    timeout: 6000ms
    connect-timeout: 6000ms
  data:
    redis:
      client-type: jedis