server:
  port: 8080
  servlet:
    context-path: /super-app


spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/app-demo-be"
    username: "postgres"
    password: "1234"
    driver-class-name: org.postgresql.Driver

  jpa:
    properties.hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9091/realms/SuperApp
idp:
  url: http://localhost:9091
  client-id: superapp
  client-secret: a8UPbI8aUTPTcTMAblwxRv7lLDS7JCRv
keycloak:
  realm: SuperApp
  admin-username: admin
  admin-password: admin123
